# 서비스를 돌리는 데 필요한 것은 Python 3.10 이상 하나뿐입니다 (pip install 불필요).
#   - urllib.request : 오픈API / 하이퍼클로바X 호출
#   - sqlite3        : 공고·판정·초안 저장
#   - http.server    : 웹 서버
#   - difflib        : 중복 공고 유사도 계산
#
# 아래는 서비스가 아니라 **골든셋 채점 도구**(agent/structure/)에만 쓰입니다.
# orchestrator.structure_notice() 를 부를 때만 로드되므로, 설치하지 않아도
# 서버는 정상적으로 뜨고 모든 화면이 동작합니다.
#   pip install "pydantic>=2,<3"
