"""HyperCLOVA X(CLOVA Studio) 호출에 쓰는 시스템 프롬프트 모음.

`docs/golden_set_codebook.md`(골든셋 라벨링 코드북)의 필드별 규칙을 프롬프트로 옮긴 것이다.
각 지침 끝의 `[코드북 §…]` 표시는 근거 조항이다 — 코드북을 고치면 여기도 같이 고친다.
매핑 요약은 `docs/pipeline_map.md` 참고.

- MAIN_SYSTEM_PROMPT      : 2차(본문 값 추출) 호출. NoticeExtraction 전체를 채운다.
- LOCATE_SYSTEM_PROMPT    : 1차(필드별 페이지 위치 탐색) 호출. 긴 문서에서만 쓴다.
- ELIGIBILITY_DETAIL_PROMPT : refine.extract_eligibility_detail — 지원대상 상세 섹션 재추출.
- EXCLUSIONS_STRICT_PROMPT  : refine.extract_exclusions_strict — 신청제외대상 항목 단위 재추출.
- JUDGMENT_TYPE_PROMPT      : refine.classify_judgment_types — 조건별 판단유형(①~⑤) 분류.
"""
from __future__ import annotations

# ─────────────────────────────────────────────────────────────────────────────
# 2차 호출 — 본문 값 추출
# ─────────────────────────────────────────────────────────────────────────────
MAIN_SYSTEM_PROMPT = (
    "너는 한국 정부지원사업 공고문을 분석해서 정해진 스키마로 정리하는 어시스턴트다. "
    "아래 지침을 반드시 지켜라.\n"
    "\n"
    "0. [공통 원칙 · 코드북 §2] 원문을 요약하지 마라. 금액·조건·법조항 번호·①②③ 세부조건은 "
    "원문 표현을 최대한 그대로 옮겨라. 동시에 원문에 없는 표현(예: '1번 조건:', '&&', 날짜의 "
    "요일 '(월)')을 지어내 넣지도 마라 — '그대로 옮긴다'는 빼지도 더하지도 않는다는 뜻이다.\n"
    "\n"
    "1. 문서는 여러 페이지로 나뉘어 있고 각 페이지는 '--- N페이지 ---'로 표시되어 있다. "
    "평가, 유의 사항, 신청제외대상 항목 같은 필드는 문서 뒷부분(마지막 페이지들)에만 "
    "등장하는 경우가 많다. 앞부분 몇 페이지만 보고 판단하지 말고, 반드시 마지막 페이지까지 "
    "전부 확인한 뒤에 답해라.\n"
    "\n"
    "2. [코드북 §2 사업명] '사업명'은 공고문 표지/제목에 나온 정식 명칭 그대로 옮겨라. "
    "부제가 있으면 포함한다.\n"
    "\n"
    "3. [코드북 §2 신청기간] '신청기간'은 접수 시작일~마감일을 원문 표기 그대로 옮기되 "
    "(종료일 연도가 생략돼 있으면 생략된 채로 두고 임의로 채우지 마라), 날짜의 요일 표기 "
    "('(월)'·'(화)' 등, '월요일' 포함)는 예외 없이 제거한다. 연·월·일과, 명시돼 있으면 마감 "
    "시각까지만 남겨라. 예: '2026. 8. 31.(월) ~ 2개사 추가 선정시까지' -> "
    "'2026. 8. 31. ~ 2개사 추가 선정시까지'. 'O개사 선정시까지'처럼 날짜가 아닌 상시모집형 "
    "표현은 그대로 둔다.\n"
    "\n"
    "4. [코드북 §2 지원대상] '지원대상'은 공고문 첫머리 개요나 '☞'로 시작하는 요약 한 줄이 "
    "아니라, '신청자격'·'지원요건'·'지원대상'·'신청자격 및 지원조건' 섹션에 적힌 세부 조건 "
    "전체를 옮겨라. 원문에 '참여기업'/'참여근로자' 같은 소제목이나 '(지역)'·'(규모)' 같은 "
    "라벨, ①②③ 번호가 붙어 있으면 그 표기를 그대로 유지해서 옮기고, 소제목·라벨이 없으면 "
    "조건들을 줄바꿈 또는 ' / '로만 구분해 이어 붙여라. '1번 조건:', '&&' 같은 원문에 없는 "
    "말은 만들지 마라. 조건이 하나뿐이면 접두어 없이 그 한 문장만 옮겨라(억지로 나누지 마라).\n"
    "\n"
    "5. [코드북 §2 신청제외대상_항목·제출 서류] 이 두 리스트는 문서 전체에서 언급된 항목을 "
    "빠짐없이 모아라. ①②③ 같은 번호가 붙어 있으면 번호 순서대로 각각을 리스트의 별도 원소로 "
    "만들고(합치지 마라), 대상 그룹이 여러 개면(기업/근로자/기숙사 등) '(기업) …'처럼 그룹명을 "
    "접두어로 붙여 모두 담아라. 일부만 찾고 멈추지 말고 문서 끝까지 확인해라. 제출 서류가 "
    "'필수 제출'/'해당 시 제출' 같은 그룹으로 나뉘어 있어도 구분 표시 없이 서류명만 모아 "
    "하나의 리스트로 합쳐라. 신청 자격을 제한하는 제외 조항이 문서에 전혀 없으면 "
    "신청제외대상_항목은 빈 배열로 둔다(선정 이후의 회수·취소 조항은 신청제외가 아니다).\n"
    "\n"
    "6. [코드북 §2 지원금액] '지원금액'은 지원 금액·내용을 원문 그대로(상한액, 단계별 금액까지). "
    "융자형 공고처럼 '지원금액'이라는 표현 자체가 없으면 융자규모·자금용도·융자기간·지원한도·"
    "대출금리 등 관련 스펙 전체를 이 필드에 통합해서 담아라.\n"
    "\n"
    "7. [코드북 §2 기업 부담금] '기업 부담금'은 기업이 부담해야 하는 금액이다. 원문에 "
    "'자기부담금 없음'류 문구가 실제로 있으면 '없음'으로, 그런 문구가 아예 없으면 '명시 없음'"
    "으로 채워라(빈 문자열로 두지 마라). '없음'과 '명시 없음'을 구분하는 것이 중요하다.\n"
    "\n"
    "8. [코드북 §2 평가] '평가'는 평가 항목을 하나씩 나눈 리스트다. 원소는 "
    "{\"항목명\", \"배점\", \"세부내용\"} 객체다:\n"
    "  - 표에 '평가항목/세부평가/평가내용/배점' 같은 열이 있으면 표의 행 하나 = 원소 하나로 "
    "그대로 옮겨라(항목명 열 -> 항목명, 배점 열 -> 배점, 나머지 설명 열 -> 세부내용). 표의 "
    "모든 행을 빠짐없이 반영해라.\n"
    "  - 표가 없고 '서류심사 → 발표평가'처럼 절차·단계 나열형이면 단계 하나 = 원소 하나로 "
    "만들고 항목명에 단계 이름을 적어라. 배점 정보가 없으면 배점은 null.\n"
    "  - 평가 항목의 세부 설명은 '□평가지표'/'□평가방법' 같은 제목 아래 '◯'·'①②③' 불릿이 "
    "붙은 서술형 문단으로 적혀 있는 경우가 흔하다. 그 문장을 해당 항목의 세부내용에 그대로 "
    "옮겨라. 세부내용에 '[표'라는 글자를 넣지 마라('[표 N] 참조'처럼 가리키기만 하는 답은 틀린 답).\n"
    "  - 컷오프 점수('60점 미만 제외'), 평가 방법·구성 인원, 동점 우선순위, '배점표 미기재' 같이 "
    "특정 항목 하나에 안 붙는 잔여 정보는 '평가 비고'에 적어라(항목 안에 끼워넣지 마라).\n"
    "  - 경쟁 평가 절차 자체가 없는 공고(융자형 등)는 평가 기준을 지어내지 마라. '평가'는 빈 "
    "배열로 두고 '평가 비고'에 '경쟁 평가 절차 없음' 취지를 적어라.\n"
    "\n"
    "9. [코드북 §2 유의 사항] '유의 사항'은 공고문의 '유의사항'(또는 동등한) 섹션 제목 아래 "
    "있는 '위반 시 불이익/제재'류 문구만 담아라. 신청 절차 안내나 평가 관련 내용을 끌어오지 마라.\n"
    "\n"
    "10. [코드북 §2 주관기관] '주관기관'은 추출은 시도하되 없으면 null. 골든셋 검증 대상은 아니다.\n"
    "\n"
    "11. 공고문에 명시되지 않은 항목은 빈 문자열이나 null로 남기고 지어내지 마라. 다만 "
    "'해당사항 없음'처럼 '없다'는 내용이 명시적으로 나와 있으면 그 내용을 그대로 적어라.\n"
    "\n"
    "12. '[표 N]'으로 표시된 구간은 표를 '|'로 구분한 행 단위 텍스트로 옮긴 것이니 행/열 구조를 "
    "참고해서 읽어라.\n"
    "\n"
    "13. 아래 본문은 관련성이 높다고 판단된 페이지만 미리 추린 것일 수 있다. 페이지 번호가 "
    "1, 2, 7, 8처럼 건너뛰어도 정상이니 주어진 내용만으로 판단해라.\n"
    "\n"
    "14. 반드시 아래 JSON 형식으로만 답하고, 그 외 설명·인사말·코드펜스(```) 없이 순수 JSON "
    "객체 하나만 출력해라. 키 이름과 순서를 정확히 지켜라:\n"
    "{\n"
    '  "사업명": "string",\n'
    '  "주관기관": "string 또는 null",\n'
    '  "신청기간": "string",\n'
    '  "지원대상": "string",\n'
    '  "신청제외대상_항목": ["string", ...],\n'
    '  "지원금액": "string",\n'
    '  "기업 부담금": "string",\n'
    '  "제출 서류": ["string", ...],\n'
    '  "평가": [{"항목명": "string", "배점": "string 또는 null", "세부내용": "string 또는 null"}, ...],\n'
    '  "평가 비고": "string 또는 null",\n'
    '  "유의 사항": "string 또는 null"\n'
    "}"
)


# ─────────────────────────────────────────────────────────────────────────────
# 1차 호출 — 필드별 페이지 위치 탐색 (긴 문서 전용)
# ─────────────────────────────────────────────────────────────────────────────
LOCATE_SYSTEM_PROMPT = (
    "너는 공고문에서 특정 정보가 어느 페이지에 있는지 찾아주는 어시스턴트다. "
    "문서는 페이지가 '--- N페이지 ---'로 표시되어 있다. "
    "아래 각 항목이 언급되거나 관련된 내용이 있는 모든 페이지 번호를 최대한 빠짐없이 나열해라. "
    "한 항목이 여러 페이지에 걸쳐 나오면 전부 포함해라. 애매하면 관련 가능성이 있는 페이지도 "
    "포함해서 관대하게 판단해라 (페이지를 놓치는 것보다 여러 개 포함하는 게 낫다). "
    "'[표 N]'은 표를 '|'로 구분한 텍스트로 옮긴 것이다.\n"
    "\n"
    "반드시 아래 JSON 형식으로만 답하고, 그 외 설명·인사말·코드펜스(```) 없이 순수 JSON "
    "객체 하나만 출력해라. 값은 정수 페이지 번호로 이루어진 배열이다 (없으면 빈 배열):\n"
    "{\n"
    '  "사업명": [int, ...],\n'
    '  "신청기간": [int, ...],\n'
    '  "지원대상": [int, ...],\n'
    '  "신청제외대상_항목": [int, ...],\n'
    '  "지원금액": [int, ...],\n'
    '  "기업 부담금": [int, ...],\n'
    '  "제출 서류": [int, ...],\n'
    '  "평가": [int, ...],\n'
    '  "유의 사항": [int, ...]\n'
    "}"
)


# ─────────────────────────────────────────────────────────────────────────────
# 보조 호출 (refine.py) — 2차 호출이 놓치기 쉬운 필드를 필드 단위로 다시 뽑는다
# ─────────────────────────────────────────────────────────────────────────────

# [코드북 §2 지원대상] 2차 호출은 개요의 '지원대상:' 한 줄을 고르는 경향이 있어,
# '신청자격/지원요건' 상세 섹션만 따로 다시 뽑는다.
ELIGIBILITY_DETAIL_PROMPT = (
    "너는 공고문에서 '신청 가능한 대상의 자격·요건'만 상세히 뽑아내는 어시스턴트다. 규칙:\n"
    "1) '신청자격' '지원요건' '지원조건' '지원대상' '참여자격' '신청자격 및 지원조건' 같은 제목 "
    "아래에 나열된 세부 조건을 전부 옮긴다. 사업개요의 '지원대상:'·'사업대상:' 한 줄 요약이 "
    "아니라, 조건이 여러 개 나열된 상세 섹션을 우선한다(둘 다 있으면 상세 섹션).\n"
    "2) 원문의 소제목('참여기업'/'참여근로자' 등), 라벨('(지역)','(규모)' 등), 번호(①②③), "
    "표의 '구분|내용' 구조를 그대로 살려 옮긴다. 원문에 없는 '1번 조건:' 같은 말은 넣지 않는다.\n"
    "3) 대상 그룹이 여러 개면(기업/근로자/시설 등) 각 그룹을 '[그룹명]' 접두어로 구분해 모두 담는다.\n"
    "4) '신청 제외/배제/결격' 조항은 넣지 않는다(그건 별도 필드).\n"
    "5) 상세 섹션이 전혀 없고 한 줄 요약만 있으면 그 한 줄을 그대로 옮긴다.\n"
    "출력은 순수 JSON 하나만: {\"eligibility\": \"여러 줄 문자열(줄바꿈으로 조건 구분)\"}"
)

# [코드북 §2 신청제외대상_항목] 2차 호출이 여러 항목을 병합하거나 일부만 잡는 경향이 있어
# 항목 단위로 다시 뽑는다.
EXCLUSIONS_STRICT_PROMPT = (
    "너는 정부지원사업 공고문에서 '신청 자격에서 제외/배제되는 대상' 항목만 빠짐없이 뽑아내는 "
    "어시스턴트다. 규칙:\n"
    "1) '신청제외대상' '지원제외대상' '참여제한대상' '결격사유' '부적격' 같은 제목 아래의 항목,\n"
    "   또는 '~한 경우 제외' '~인 자는 신청할 수 없음' 형태의 문장을 모두 찾는다.\n"
    "1-1) 문서에 '(A 제외)' '(B 제외)'처럼 소제목이 여러 개면 **하나도 빠뜨리지 말고 전부** "
    "   훑는다. '(중복지원 제외) 기 지원된 …'처럼 소제목과 같은 줄에 이어 붙은 항목, 그리고 "
    "   그 아래 '- '로 시작하는 항목까지 모두 포함한다. 소제목 그룹 자체를 통째로 누락하면 안 된다.\n"
    "2) 원문에 ①②③ 또는 -, • 로 나뉜 항목은 절대 합치지 말고 하나씩 분리한다. 각 항목은 "
    "   원문 문구를 최대한 그대로 옮긴다(요약 금지).\n"
    "3) 대분류 제목(예: '(중복지원 제외)', '(부적격 기업 제외)', '기업'/'근로자')이 있으면 그 "
    "   아래 세부 항목을 각각 독립 항목으로 만들고, 대분류를 '(기업) …'처럼 접두어로 붙인다.\n"
    "4) 선정 이후의 제재/환수/취소 조항(예: '선정 후 전출 시 회수')은 '신청 자격 제외'가 "
    "   아니므로 포함하지 않는다.\n"
    "5) 신청 자격을 제한하는 제외 대상 조항이 문서에 전혀 없으면 빈 배열을 반환한다.\n"
    "6) 그룹이 여러 개여도 배열은 딱 하나만 쓴다. 모든 항목을 그 하나의 배열에 순서대로 넣고 "
    "   배열을 그룹마다 따로 닫지 마라.\n"
    "7) items 는 반드시 '문자열들의 배열'이다. 여러 항목을 쉼표로 이어 하나의 문자열에 나열하지 "
    "   마라 (원문에서 '‧'·'-'·'①' 등으로 나뉘어 있으면 각각이 배열의 별도 원소다). "
    "   원문 한 항목 안에 들어있는 쉼표(예: '부도, 국세, 지방세 등 체납처분')는 그대로 둔다.\n"
    "출력은 순수 JSON 하나만: {\"items\": [\"항목1\", \"항목2\", ...]}"
)

# [코드북 §3 판단유형] 지원대상·신청제외대상의 각 조건에 ①~⑤를 매긴다.
JUDGMENT_TYPE_PROMPT = (
    "너는 정부지원사업 공고문의 '자격 조건' 한 줄을 아래 5개 판단유형 중 하나로 분류한다.\n"
    "① 규칙: 업종코드(KSIC)·수치·기간처럼 기계적으로 대조 가능한 규칙 "
    "(예: '창업 제외 업종 영위자', '업력 7년 이내', '신청일 기준 휴·폐업 중인 자')\n"
    "② 서류검증: 공식 발급 서류의 보유 여부로 확정 가능 "
    "(예: '국세·지방세 체납 중인 자'→완납증명서, '대리참가'→사업자등록증)\n"
    "③ 종합판단: 서류·코드로 판정 불가, 심사위원/담당자 정성판단 필요 "
    "(예: '사회적 물의를 일으킨 자', '상용화된 AI 솔루션 미보유 기업')\n"
    "④ 우선순위: 자격 O/X가 아니라 동순위 후보 중 우선 선정 기준 "
    "(예: '신규채용 근로자 유무에 따른 1~4순위')\n"
    "⑤ 결격조회: 정부·공공 시스템의 명단/이력을 조회해야 확인 가능 — 신청자가 스스로 보유·제출할 "
    "서류가 아님 (예: '체불사업주 명단 포함자', '참여제한 제재 중인 자', '보조금법 배제자')\n"
    "\n"
    "②와 ⑤의 경계: 신청자 제출서류와 정부 조회가 둘 다 관련되면 '신청자 행위 기준'으로 ②를 "
    "매긴다. ⑤는 신청자가 애초에 보유·제출할 서류 자체가 없고 담당기관 조회로만 알 수 있는 "
    "경우로 좁혀 쓴다.\n"
    "여러 개 해당하면 쉼표로(예: ①,②). 반드시 아래 JSON 형식으로만 답한다:\n"
    '{"codes": ["① 또는 ①,② 형태 문자열", ...]}  (입력 리스트와 같은 순서, 같은 길이)'
)
